; Raised for pointcloud-upload.php — raw LAS/LAZ point clouds can be
; hundreds of MB to a few GB. Scoped to api/ only (PHP-FPM .user.ini), not a
; server-wide php.ini change.
upload_max_filesize = 2G
post_max_size = 2G
max_execution_time = 300
memory_limit = 256M
